# Task Implementation Report — T3_6

**Task ID**: T3_6 | **Title**: Unit tests: server TLS injection | **Phase**: 3

Added `TestGenerateServerConfMap_CentralTLSInjection` and `TestGenerateSpireControllerManagerConfigYaml_CentralTLSInjection` covering inject/skip paths and hash delta.

**Verify**: `go test ./pkg/controller/spire-server/... -run CentralTLS` PASSED
